{% extends "TroiswaFrontBundle::layout-front.html.twig" %}

{% block title %}
    Nous contacter
{% endblock %}

{% block content %}
    <h1>Nous contacter</h1>
    <div class="well col-lg-5">
        <div class="form-group">
            {{ form_start(form_contact) }}
            {{ form_row(form_contact.firstname, {label: 'Prénom'}) }}
            {{ form_row(form_contact.lastname, {label: 'Nom'}) }}
            {{ form_row(form_contact.email, {label: 'Mail'}) }}
            {{ form_row(form_contact.subject, {label: 'Sujet'}) }}
            {{ form_row(form_contact.contenu, {label: 'Message'}) }}
            {{ form_end(form_contact) }}
        </div>
    </div>
{% endblock %}